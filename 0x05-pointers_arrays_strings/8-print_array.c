#include "main.h"
/**
 * print_array- print array
 * @a: input
 * @n: input
 * Return: void
 */

void print_array(int *a, int n)
{
	int ar[0] = *a;
	int i = 0;

	while (*s != '\0' && i < n)
	{
		printf("%d, ", ar[i]);
		i++;
		s++;
	}
	_putchar('\n');
}
